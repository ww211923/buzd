# 🚀 DeepSeek AI + 宝塔面板智能管理系统开发需求

---

## 📋 项目概述

本项目是一个基于Web的智能服务器管理系统，通过自然语言对话方式，让用户可以使用DeepSeek AI来管理宝塔面板服务器。

**核心理念**: 用户说需求 → AI智能分析 → 自动调用宝塔API → 返回执行结果

---

## 🏗️ 技术架构

| 层级 | 技术栈 | 说明 |
|------|--------|------|
| 前端 | HTML5 + CSS3 + JavaScript | 原生开发，无框架依赖 |
| 后端 | PHP 7.3+ | API接口，处理业务逻辑 |
| AI服务 | DeepSeek API | 自然语言理解和响应生成 |
| 面板对接 | 宝塔面板API | 服务器管理操作 |

---

## 🎯 核心设计理念

### AI Agent模式

```
用户输入 → AI分析需求 → 智能决策调用哪个API → 执行操作 → 返回结果
```

AI需要能够：
1. 理解用户的自然语言需求
2. 分析需要调用哪些宝塔API
3. 自动执行并返回结果

---

## 🔌 API对接规范

### 宝塔面板API

| 参数 | 值 |
|------|-----|
| **面板地址** | `https://38.207.177.103:35957` |
| **API Key** | `BjdFTLZ2jbQ856A4wjl55lnA7uOInsTu` |
| **签名算法** | `md5(request_time + md5(api_key))` |

### 宝塔API列表（供AI智能调用）

| API方法 | 功能描述 | 典型场景 |
|---------|---------|---------|
| `getSystemTotal()` | 获取系统状态（CPU/内存/运行时间） | 用户说"查看服务器状态" |
| `getSites()` | 获取网站列表 | 用户说"查看网站" |
| `addSite(domain, path, php)` | 创建网站 | 用户说"创建网站 example.com" |
| `deleteSite(siteId)` | 删除网站 | 用户说"删除网站 example.com" |
| `getDatabaseList()` | 获取数据库列表 | 用户说"查看数据库" |
| `addDatabase(name)` | 创建数据库 | 用户说"创建数据库 mydb" |
| `deleteDatabase(name)` | 删除数据库 | 用户说"删除数据库 mydb" |
| `executeCommand(cmd)` | 执行Shell命令 | 用户说"执行命令 ls -la" |
| `getFilesList(path)` | 获取文件列表 | 用户说"查看目录" |
| `readFile(path)` | 读取文件内容 | 用户说"查看文件内容" |
| `writeFile(path, content)` | 写入文件 | 用户说"创建文件 xxx" |

### DeepSeek API

| 参数 | 值 |
|------|-----|
| **接口地址** | `https://api.deepseek.com/chat/completions` |
| **模型** | `deepseek-chat` |
| **认证方式** | Bearer Token |

---

## 🎨 界面设计要求

### 设计风格

- **主题**: 极简液态玻璃风格
- **背景**: 紫色渐变 (`#667eea → #764ba2`)
- **效果**: 毛玻璃卡片 (`backdrop-filter: blur`)
- **边角**: 圆润设计（12-16px圆角）

### 布局结构

```
┌─────────────────────────────────────┐
│  🤖 DeepSeek + 宝塔 AI  │ ⚙️ 设置   │  ← 顶部导航栏
├─────────────────────────────────────┤
│  [CPU: 1%]  [内存: 42%]  [运行: 30天]  ← 状态栏
├─────────────────────────────────────┤
│  📊状态  🌐网站  💻命令  📁文件  ➕建站  ← 快捷命令
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐   │
│  │ 👋 欢迎！我是您的宝塔AI助手...  │   │
│  └─────────────────────────────┘   │
│  ┌─────────────────────────────┐   │
│  │ 用户消息（紫色气泡）          │   │  ← 聊天区域
│  └─────────────────────────────┘   │
│  ┌─────────────────────────────┐   │
│  │ AI响应（浅色气泡）            │   │
│  └─────────────────────────────┘   │
├─────────────────────────────────────┤
│  [输入框]  [发送按钮]              ← 输入区域
└─────────────────────────────────────┘
```

### 交互体验

- 消息气泡动画效果
- 实时状态更新
- 系统提示消息（🧠 正在处理您的请求...）
- 快捷命令一键点击

### 移动端适配

- 响应式布局
- 触摸友好的按钮尺寸（最小44px）
- 流畅的滑动体验

---

## 📁 项目结构

```
/
├── index.php          # 主页面（前端界面）
├── api.php            # API接口（后端）
├── includes/
│   └── BaoTaAPI.php   # 宝塔API封装类
└── test.html          # API测试页面（可选）
```

---

## ✅ 技术要求

| 要求 | 说明 |
|------|------|
| PHP版本 | 7.3+ 兼容 |
| 框架依赖 | 无（原生PHP/JS） |
| 跨域支持 | CORS |
| 超时时间 | 300秒（长任务支持） |
| 配置存储 | localStorage |

---

## 📝 示例对话流程

```
用户: "帮我创建一个网站，域名为 example.com"
AI:   "好的，我来为您创建网站。【创建网站：example.com】"
系统: 执行 addSite("example.com", "/www/wwwroot/example.com", "73")
AI:   "✅ 网站创建成功！域名: example.com"
```

```
用户: "查看当前服务器状态"
AI:   "【查看系统状态】"
系统: 执行 getSystemTotal()
AI:   "📊 服务器状态：CPU 1%，内存 42%，已运行 30 天"
```

---

## 🔧 部署说明

1. **环境要求**: PHP 7.3+ + Nginx/Apache
2. **配置文件**: 无需额外配置文件，配置通过前端界面设置
3. **端口**: 默认8080（可自行修改）

---

## 📱 界面参考

参考截图中的界面布局，实现类似的液态玻璃风格聊天界面。

---

## 📮 交付物

- [ ] `index.php` - 前端界面
- [ ] `api.php` - 后端API接口
- [ ] `includes/BaoTaAPI.php` - 宝塔API封装类
- [ ] `test.html` - 测试页面（可选）

---

**请实现这个系统，让AI能够智能分析用户需求并自动调用相应的宝塔API！**